# PROJECT.md

Standing scope document for this project. Briefs should point back to this file rather than restating decisions already made here — update it when a brief resolves a new decision (features, stack, storage, etc.), the same way a session would.

## Overview

A small worked example of using this framework and skill for developing code

## Core scope

A function that calculates GC content. Must have a flag to switch between 
returning the output as a proportion and percent. 

## Out of scope

Tooling or wrapping this in a script - this is a standalone function
Handling NA characters is not important at this point in time

## Stack & tooling

Python standard libraries only

## Data & storage

Manual strings, nothing more complicated